{
  "name": "@totemsdk/tx-builder",
  "version": "0.1.9",
  "description": "Transaction builder for Minima blockchain — coin selection, multisig, and WOTS signing",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.js",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "npm run build:wasm && tsc && rm -rf dist/__tests__ dist/tx-builder dist/core && node ../../scripts/clean-wasm-output-ignores.mjs",
    "build:wasm": "cd rust && wasm-pack build --target bundler --out-dir pkg && wasm-pack build --target nodejs --out-dir pkg-node",
    "prepare": "npm run build:wasm && tsc && rm -rf dist/__tests__ dist/tx-builder dist/core && node ../../scripts/clean-wasm-output-ignores.mjs",
    "prepack": "rm -rf dist/__tests__ dist/tx-builder dist/core",
    "clean": "rm -rf dist rust/pkg rust/pkg-node rust/target",
    "test": "jest --config jest.config.cjs",
    "test:rust": "cd rust && cargo test"
  },
  "files": [
    "dist",
    "rust/pkg",
    "rust/pkg-node",
    "README.md",
    "LICENSE"
  ],
  "dependencies": {
    "@totemsdk/core": "workspace:^"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "author": "Totem SDK",
  "homepage": "https://totem.ing",
  "bugs": {
    "url": "https://github.com/Totem-Edge/totem-sdk/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Totem-Edge/totem-sdk.git",
    "directory": "packages/tx-builder"
  },
  "keywords": [
    "totem",
    "totemsdk",
    "minima",
    "blockchain",
    "quantum-resistant",
    "wots",
    "utxo"
  ]
}
