import { describe, it, expect } from '@jest/globals'

describe('edge-lorawan', () => {
  it('package can be imported', () => {
    // Placeholder: adapter smoke test
    expect(true).toBe(true)
  })
})
