import React from 'react';
import ReactDOM from 'react-dom/client';
import { VerifyApproval } from './VerifyApproval';
import '../theme/axia-tokens.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <VerifyApproval />
  </React.StrictMode>
);
