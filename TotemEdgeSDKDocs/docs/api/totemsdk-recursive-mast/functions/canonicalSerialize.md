[**@totemsdk/recursive-mast**](../index.md)

***

[@totemsdk/recursive-mast](../index.md) / canonicalSerialize

# Function: canonicalSerialize()

> **canonicalSerialize**(`domain`, `payload`, `version?`): `Uint8Array`

## Parameters

### domain

[`EncodingDomain`](../type-aliases/EncodingDomain.md)

### payload

`Record`\<`string`, `unknown`\>

### version?

`number` = `CANONICAL_ENCODING_VERSION`

## Returns

`Uint8Array`
