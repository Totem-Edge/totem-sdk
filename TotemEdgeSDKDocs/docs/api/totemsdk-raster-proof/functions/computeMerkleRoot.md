[**@totemsdk/raster-proof**](../index.md)

***

[@totemsdk/raster-proof](../index.md) / computeMerkleRoot

# Function: computeMerkleRoot()

> **computeMerkleRoot**(`chunks`): `string`

Compute the Merkle root over chunk hashes (deterministic). Odd layers
promote the last hash unchanged.

## Parameters

### chunks

[`RasterChunk`](../interfaces/RasterChunk.md)[]

## Returns

`string`
