[**@totemsdk/kissvm**](../index.md)

***

[@totemsdk/kissvm](../index.md) / buildDelegatedCredentialScript

# Function: buildDelegatedCredentialScript()

> **buildDelegatedCredentialScript**(`config`): `string`

Build a short-lived delegated credential script.
Credentials are not encoded indefinitely — they expire, limiting the
damage from lost or compromised keys.

## Parameters

### config

[`DelegatedCredentialConfig`](../interfaces/DelegatedCredentialConfig.md)

## Returns

`string`
