[**@totemsdk/spatial-proof**](../index.md)

***

[@totemsdk/spatial-proof](../index.md) / isRingClosed

# Function: isRingClosed()

> **isRingClosed**(`ring`): `boolean`

Ring closure: the first and last points must be equal. When a ring is not
closed, it is rejected (see normalizePolygonRing for the deterministic
normalizer).

## Parameters

### ring

[`Coordinate`](../type-aliases/Coordinate.md)[]

## Returns

`boolean`
